import { Stack } from "@mui/material"

const PopularProperties = () => {
    return(
        <Stack className="container">
            <div>Popular Property</div>
        </Stack>
    )
};

export default PopularProperties;